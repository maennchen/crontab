language: elixir
elixir:
  - 1.3
  - 1.4
notifications:
  email:
    on_success: change
    on_failure: change
after_script:
  - MIX_ENV=docs mix deps.get
  - MIX_ENV=docs mix inch.report
  - MIX_ENV=test mix coveralls.travis
